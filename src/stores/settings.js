import { ref } from "vue";

export const settings = ref({
    settingsMode: false,
    tutorialMode: false,
    shareMode: false,
    graphType: "2d",
    xAxis: true,
    yAxis: true,
    zAxis: true,
    gridLines: true,
})

